{
	"name": "@cheaaa/icons",
	"version": "1.9.0",
	"description": "<>",
	"build:storybook": "src/index.ts",
	"main": "dist/index.js",
	"module": "dist/input.esm.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"test": " mocha 'tests/**/*.test.{js,ts,tsx}'",
		"test2": "jest",
		"createStoriesFile": "node ./scripts/createStoriesFile.js",
		"compileNew": "node build",
		"compileNew1": "npx tsc",
		"compileOld": "tsdx build --format cjs,esm --verbose",
		"compile": "yarn compileNew1",
		"build": "yarn clean && yarn compile",
		"build:storybook": "yarn createStoriesFile && yarn build",
		"prepublish": "node ./scripts/prepublish.js",
		"clean": "rimraf dist lib .publish node_modules"
	},
	"author": "Cherehapa",
	"license": "MIT",
	"publishConfig": {
		"access": "public"
	},
	"private": false,
	"devDependencies": {
		"react-use": "^17.4.0"
	},
	"peerDependencies": {
		"react": ">=17.0.2"
	}
}
